import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EmailScreenController = () => {
  return (
    <View>
      <Text>EmailScreenController</Text>
    </View>
  );
};

export default EmailScreenController;

const styles = StyleSheet.create({});
