// Type of Border
export enum BorderType {
  DOTTED = 1,
  SOLID = 2,
  DASHED = 3,
}

// Style of Border
export enum BorderStyle {
  DOTTED = 'dotted',
  DASHED = 'dashed',
  SOLID = 'solid',
}

// Render Image Source Type
export enum ImageSourceType {
  URL = 'Url',
  LOCAL = 'Local',
}
